@AGENTS.md

# プロジェクトルール

## HTMLプレビューの同期
アプリ（src/ 以下）に機能追加・修正を行ったときは、必ず以下のHTMLプレビューファイルも同じ変更を反映すること：

- `demo.html` — メインのデモファイル（常に最新の状態を保つ）
- `preview-device-form.html` — 機器登録フォームのプレビュー

機能の追加・変更・削除があるたびに、対応するHTMLプレビューを更新してからコミットすること。
